<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for dark mode. -->
    <style name="Base.Theme.MockupTaller" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/my_dark_primary</item>
        <item name="colorOnPrimary">@color/my_dark_on_primary</item>
        <item name="colorSurface">@color/my_dark_surface</item>
        <item name="colorOnSurface">@color/my_dark_on_surface</item>
        <!-- Add more theme attributes for the dark theme here. -->
    </style>

    <!-- Optional: Add a custom dark theme if needed -->
    <style name="Theme.custom" parent="Base.Theme.MockupTaller">
        <item name="android:textAllCaps">false</item>
    </style>
</resources>
